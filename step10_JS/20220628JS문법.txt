1. 변수
    var 변수명;
    let 변수명;
    const 변수명;
    변수명;
    함수의 parameter로 변수 선언
        function 함수명(변수명, 변수명2) {
            ..
        }

    1-2. 호출
        변수명만 호출

1-2. 사용자 정의 일반 함수 구현
    1-2-1. 구현
    function 함수명([변수명1, ...]) {
        처리 로직
        [return 값]
    }

    1-2-2. 호출
        함수명();
        함수명(parameter..);

        